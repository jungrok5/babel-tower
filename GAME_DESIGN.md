# Babel Tower — 게임 기획서

> "기기를 최대한 움직이지 않고 탑을 쌓는 게임."
> 탑이 흔들리는 게 아니라, **나의 손**이 흔들려서 탑이 무너진다.

---

## 1. 한 줄 정의

**정지(Stillness)를 강요하는** 바벨탑 스택 게임. 블록을 쌓는 게임이 아니라,
**자기 자신(손의 떨림)과 싸우는 인내의 게임.**

기존 자이로 게임이 "기울여서 조종(Tilt to Control)"하는 것과 정반대로,
"움직이지 마라(Stay Still)"를 핵심 메카닉으로 삼는다.

## 2. 차별점 (Blue Ocean)

| 기존 스택 게임 | 기존 자이로 게임 | **Babel Tower** |
|---|---|---|
| 터치 타이밍 | 기울여 조준/이동 | **기기를 정지시켜라** |
| 반사신경 | 조작 숙련 | **호흡·집중·심리 통제** |

- **물리적 일체감**: 화면 속 탑이 아니라 내 손이 흔들려 무너진다 → 죄책감 + 긴장감.
- **몰입**: 무언가를 지키려 할 때 인간은 극도로 집중한다. 그 찰나의 호흡까지 게임이 된다.
- **테마 적합성**: 바벨탑의 '필연적 붕괴'를 전달하기에 완벽한 시스템.

## 3. 코어 루프

```
보정(Calibration) → [탭 → 벽돌 낙하 → 정지 유지 → 안착] × N → 붕괴 → 무덤 → 재도전
```

- 한 벽돌당 **10~20초 초단기 집중**. 손목 피로를 고려한 짧은 승부.
- 높이 = 점수. 최고 기록과 "역대 잔해"가 축적된다.

## 4. 3단계 감정 설계

### 1단계 — 보정의 의식화
시작 시 "가장 편안한 자세로 기기를 잡으세요" 안내.
평소 손떨림/센서 드리프트를 **0점**으로 잡는 이 과정 자체가
신성한 탑을 쌓기 전의 **경건한 준비 의식**처럼 느껴지게 한다.
→ 구현: `Motion.start_calibration()` 이 기준값(baseline)을 측정·차감.

### 2단계 — 높이에 따른 '공포' 시스템
- 탑이 낮을 땐 조금 흔들려도 괜찮다.
- 높아질수록 **카메라가 줌아웃**되고, 같은 떨림도 탑 전체를 크게 요동치게 한다.
- 위로 갈수록 벽돌에 **지렛대 효과**(상단일수록 큰 힘)를 적용 → 꼭대기부터 무너진다.
- (로드맵) 배경음을 고요하게 시작해, 높아질수록 바람·군중의 웅성거림을 키워 심박수를 자극.
→ 구현: `main.gd` 의 줌 `clampf(1.0 - score*0.03, ...)`, 층별 `lever` 힘.

### 3단계 — 붕괴의 미학
- 'Game Over' 텍스트로 끝내지 않는다.
- 무너지는 순간 **강렬한 햅틱**(진동) + 카메라 셰이크.
- **"역대 유저들의 실패한 바벨탑 무덤"** 을 보여준다.
- 메시지: *"당신도 수많은 욕망의 잔해 중 하나가 되었습니다."*
→ 구현: `Input.vibrate_handheld()`, `Graveyard` (과거 높이 누적), 붕괴 오버레이.

## 5. 입력 추상화 (플랫폼 3단 폴백)

핵심 로직은 `get_sway()`(-1~1)와 `get_shake()`(0~1) **두 값만** 소비한다.
소스는 플랫폼에 따라 자동 전환된다. (`scripts/motion_sensor.gd`)

1. **네이티브(Android/iOS)** — 자이로스코프(각속도) + 중력벡터(기울기)
2. **웹 모바일** — JavaScript `devicemotion` 이벤트 (보정 화면의 "센서 켜기" 버튼에서 권한 요청)
3. **데스크톱/웹 데스크톱** — **마우스 움직임 = 손떨림.** 지금 바로 페이지에서 테스트 가능.

> 데스크톱에서 마우스를 가만히 두면 안정, 흔들면 탑이 흔들린다 — 실기기 감각의 대체 테스트.

## 6. UX / 현실적 이슈 (반드시 해결)

- **접근성(수전증 대응)**: 사람마다 떨림 정도가 다르다.
  `Motion.sensitivity` 로 허용 범위 조절 → 난이도/옵션에 노출 예정.
  (로드맵) '약물(일시적 안정)' 같은 아이템으로 밸런스 재미 부여.
- **센서 드리프트**: 자이로는 가만히 둬도 값이 미세히 변한다.
  보정으로 기준값을 차감하고, "일정 시간 무동작 시 재보정" UX를 추후 추가.
- **손목 피로**: 너무 길게 정지시키면 지친다 → 벽돌당 초단기 승부로 설계.

## 7. 재료(Material) 로드맵

> 질문: "탑을 쌓는 재료를 무엇으로?" — 재료마다 붕괴의 '질감'이 달라지면 훨씬 재미있다.

프로토타입 기본값은 **돌/벽돌**(바벨탑 테마의 무게감). 이후 재료별 물성으로 확장:

| 재료 | 물성(friction/mass/bounce) | 붕괴 질감 | 정서 |
|---|---|---|---|
| **돌/철근** (기본) | 높은 마찰·무거움 | 육중하게 기울다 와르르 | 바벨탑의 무게, 욕망 |
| **얼음** | 낮은 마찰·미끄러짐 | 스르륵 미끄러져 붕괴 | 차갑고 덧없음 |
| **모래주머니** | 물렁·감쇠 큼 | 눌리며 뭉개짐 | 허무 |
| **종이/카드** | 가벼움·바람에 취약 | 팔랑이며 날림 | 연약함 |

→ 구현 접근: `Block` 의 `PhysicsMaterial`·`mass`·색/드로우를 재료 프리셋으로 스왑.

## 8. 프로토타입 범위 vs 로드맵

**지금 구현됨 (v0.1 — 웹 테스트 가능)**
- 정지 메카닉(3단 입력 폴백), 보정 의식, 벽돌 낙하·안착, 붕괴 판정
- 높이별 줌아웃·지렛대 공포, 카메라 셰이크, 햅틱
- 무덤(과거 높이 누적)·최고 기록, 재도전

**다음 (v0.2+)**
- 사운드스케이프(고요→바람/군중), 재료 시스템, 난이도/접근성 UI
- 파티클·조명 붕괴 연출, 3D 승격 옵션, 리더보드/공유

## 9. 기술 스택

- **엔진**: Godot 4.7 (GL Compatibility 렌더러 — 웹/모바일 호환 최상)
- **웹**: HTML5 익스포트, `thread_support=false` → 특수 헤더 없이 정적 호스팅 가능
- **배포**: GitHub Actions → GitHub Pages 자동 익스포트/게시
- **모바일**: Android(.apk/.aab), iOS(.ipa) 프리셋 포함 (센서 권한 설정 완료)
